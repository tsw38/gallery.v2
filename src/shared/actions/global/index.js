import { shuffle } from './shuffle';
import * as imagesHelper from './getImages';
import * as randomHelper from './randomNumber';
import { timeout } from './timeout';

export default {
  shuffle,
  timeout,
  imagesHelper,
  randomHelper
}
