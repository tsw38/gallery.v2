import {
  Variables
} from '../../utils';

export default class Authentication {
  constructor() {
    this.domain = Variables.origin;
    // this.fetch = this.fetch.bind(this) // React binding stuff
    // this.login = this.login.bind(this)
    // this.getProfile = this.getProfile.bind(this)
  }
}
