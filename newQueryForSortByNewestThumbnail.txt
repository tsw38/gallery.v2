SELECT
  A.albumName,
  A.url,
  T.photoName
FROM
  albums as A
  LEFT JOIN photos AS P ON A.id = P.albumId
  LEFT JOIN photos AS T ON A.thumbnailId = T.photoId
GROUP BY
  A.albumName,
  A.url,
  T.photoName
ORDER BY
  max(P.timestamp) DESC
